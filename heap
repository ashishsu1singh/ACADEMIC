#include<iostream>
#include<queue>
using namespace std;
int main(){

        priority_queue<int,vector<int>,greater<int>> pq;

        priority_queue<int> maxHeap;

        pq.push(10);
        pq.push(20);
        pq.push(5);
        pq.push(30);

        cout<<pq.top()<<endl;
        int t=pq.top();
        pq.pop();
        cout<<t<<endl;
        cout<<pq.top()<<endl;
        return 0;
}
