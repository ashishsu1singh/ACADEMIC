#include <iostream>
using namespace std;

int main()
{
   int a,b ,temp=1;
   cout<<"Enter the value of the item";
   cin>>a;
   while(a%10!=0)
   {
       b=(a%10);
       temp=temp*b;
       a=a/10;
       
   }
   cout<<temp;
}
